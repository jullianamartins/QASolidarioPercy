describe('Página Principal', () => {

    it('deve acessar a página principal e clicar em um botão', () => {
      // Visita a página principal (substitua 'http://localhost:3000' pelo URL correto)
      cy.visit('http://localhost:3000');
  
      // Clica no botão (substitua 'seletor-do-botao' pelo seletor correto do botão)
     // cy.get('Acesse aqui').click();
  
      // Adicione asserções para verificar se o clique no botão teve o efeito esperado
      // Por exemplo, verificar se uma nova página foi carregada, um modal abriu, etc.
      // cy.url().should('include', '/nova-pagina'); // Exemplo de asserção de URL
    });
  });